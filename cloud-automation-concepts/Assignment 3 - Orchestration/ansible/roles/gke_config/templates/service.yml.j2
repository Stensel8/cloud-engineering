apiVersion: v1
kind: Service
metadata:
  name: {{ item.name }}-svc
  namespace: {{ namespace }}
spec:
  selector:
    app: {{ item.name }}
  ports:
    - name: http
      port: 80
      targetPort: {{ item.port }}
  type: ClusterIP
